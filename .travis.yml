language: node_js
node_js:
  - "stable"

branches:
  only:
    - master

before_script:
  - npm install -g @angular/cli
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
  - sleep 3 # give xvfb some time to start

addons:
  chrome: stable

script:
  - ng lint
  - ng test
  - ng e2e